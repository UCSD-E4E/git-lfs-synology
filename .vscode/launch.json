{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/target/debug/git-lfs-synology",
            "args": [
                "login",
                "--url",
                "https://e4e-nas.ucsd.edu:6021",
                "--user",
                "<user>"
            ],
            "cwd": "${workspaceFolder}"
        }
    ]
}